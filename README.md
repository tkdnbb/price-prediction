# price-prediction

This project uses `Python3.13.5`.

# dependencies
sea `pyproject.toml`

# APIs
`/predict`: accpet housing features, returns price predictions(both single prediction and batch predictions)

`/model-info`: returns model coefficients and performance metrics

`/health`: simple health check endpoint
